{
  "private": true,
  "scripts": {
    "install": "./install.sh",
    "test": "jest",
    "test-follow-symlinks": "jest --followSymlinks"
  },
  "devDependencies": {
    "jest": "^20.0.4"
  },
  "jest": {
    "testPathIgnorePatterns": [
      "other"
    ]
  }
}
